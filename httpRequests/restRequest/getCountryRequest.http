### REST - Spring MVC @RestController (CountryRestController), GET /api/countries/{name}, direct to CountryRepository — no Camel involved
< {%
    function randomHex(length) {
        let result = '';
        const chars = '0123456789abcdef';
        for (let i = 0; i < length; i++) {
            result += chars[Math.floor(Math.random() * chars.length)];
        }
        return result;
    }

    const traceId = randomHex(32);
    const spanId  = randomHex(16);

    request.variables.set("traceId", traceId);
    request.variables.set("spanId", spanId);
%}
GET http://localhost:8080/api/countries/Spain
Accept: application/json
# traceparent: {version}-{trace-id}-{span-id}-{trace-flags}
traceparent: 00-{{traceId}}-{{spanId}}-01
baggage: testBaggage=hallo

### REST - without traceparent
GET http://localhost:8080/api/countries/Spain
Accept: application/json
baggage: testBaggage=hallo