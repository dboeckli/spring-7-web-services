= Spring Boot 4 Webservices Project

:spring_version: current
:spring_boot_version: current
:spring_ws_api: https://docs.spring.io/spring-ws/docs/current/api
:toc:
:icons: font
:source-highlighter: prettify
:project_id: gs-soap-service

This project is based on the guide from https://github.com/spring-guides/gs-producing-web-service/commits?author=bclozel:

- https://spring.io/guides/gs/producing-web-service

== What You Will build

You will build a server that exposes data from various European countries by using a
WSDL-based SOAP web service.

NOTE: To simplify the example, you will use hardcoded data for the United Kingdom, Spain, and Poland.

== Architecture Overview

The project exposes three SOAP interface versions and one REST interface, each with a different integration approach:

[cols="1,2,3", options="header"]
|===
| Version | Entry Point | Flow

| *V1* (SOAP)
| Spring-WS `@Endpoint` (`CountrySoapEndpoint`)
| SOAP Client → Spring-WS Endpoint → `CountryRepository` (Business Logic)

| *V2* (SOAP)
| Spring-WS `@Endpoint` (`CountrySoapEndpointV2`)
| SOAP Client → Spring-WS Endpoint → Apache Camel (`direct:get-country`) → `CountryRepository` (Business Logic)

| *V3* (SOAP)
| Apache Camel `spring-ws:` component
| SOAP Client → Camel Route (`spring-ws:rootqname:getCountryRequestV3`) → Apache Camel (`direct:get-country`) → `CountryRepository` (Business Logic)

| *REST*
| Spring MVC `@RestController` (`CountryRestController`)
| HTTP Client → `GET /api/countries/{name}` → `CountryRepository` (Business Logic)
|===

Key observations:

* *V1* uses Spring-WS directly and accesses the repository without any middleware.
* *V2* uses Spring-WS as the entry point but delegates processing to a shared Apache Camel route (`direct:get-country`) via a `ProducerTemplate`. This allows reuse of the Camel-based processing pipeline.
* *V3* uses Apache Camel's `spring-ws` component as the entry point, so the entire request handling — including SOAP message reception — is managed by Camel. It routes into the same shared `direct:get-country` route as V2.
* The shared Camel route (`direct:get-country`) handles JAXB marshalling/unmarshalling and OpenTelemetry baggage propagation for both V2 and V3.
* The *REST* interface bypasses both Spring-WS and Camel entirely, accessing the repository directly like V1.

.Interface Architecture
....
      SOAP Clients                                         HTTP Client
           │                                                    │
    ┌──────┼──────────────────────────┐                         │
    │ V1   │ V2                       │ V3                      │
    ▼      ▼                          ▼                         ▼
┌─────────────┐ ┌─────────────┐ ┌───────────────┐ ┌──────────────────────┐
│  Spring-WS  │ │  Spring-WS  │ │ Apache Camel  │ │     Spring MVC       │
│  @Endpoint  │ │  @Endpoint  │ │ spring-ws:    │ │   @RestController    │
│    (V1)     │ │    (V2)     │ │ rootqname(V3) │ │ /api/countries/{n}   │
└──────┬──────┘ └──────┬──────┘ └──────┬────────┘ └──────────┬───────────┘
       │ direct         │ via           │            direct    │
       │                │ ProducerTpl   │                      │
       │                └──────┬────────┘                      │
       │                       │                               │
       │          ┌────────────▼──────────────┐                │
       │          │     Apache Camel Route    │                │
       │          │     direct:get-country    │                │
       │          │  (JAXB marshal + OTel)    │                │
       │          └────────────┬──────────────┘                │
       │                       │                               │
       └───────────────────────┤    ┌──────────────────────────┘
                               ▼    ▼
                      ┌─────────────────────┐
                      │  CountryRepository  │
                      │  (Business Logic)   │
                      └─────────────────────┘
....


== SOAP Message Structure

A SOAP message comprises 4 elements:

....
+------------------------------------------------------------------+
| <soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/">    |
|                                                                  |
|  +------------------------------------------------------------+  |
|  | SOAP Header                                                |  |
|  |                                                            |  |
|  |  <soap:Header>                                             |  |
|  |    Optional header parts                                   |  |
|  |  </soap:Header>                                            |  |
|  |                                                            |  |
|  +------------------------------------------------------------+  |
|                                                                  |
|  +------------------------------------------------------------+  |
|  | SOAP Body                                                  |  |
|  |                                                            |  |
|  |  <soap:Body>                                               |  |
|  |    SOAP Message Payload                                    |  |
|  |    Optional SOAP Faults                                    |  |
|  |  </soap:Body>                                              |  |
|  |                                                            |  |
|  +------------------------------------------------------------+  |
|                                                                  |
+------------------------------------------------------------------+
....

Key characteristics of SOAP messages in this web service:

* The request messages (like `getCountryRequest`) are sent in the SOAP Body.
* The responses (like `getCountryResponse`) are returned in the SOAP Body
* Any errors would be returned as SOAP Faults in the response

== Add WSDL runtime dependency

The service will require the wsdl4j dependency:

The original wsdl4j library (which apparently is not maintained anymore) has been replaced with libre-wsdl4j:

- https://github.com/librewsdl4j/libre-wsdl4j

== Create an XML Schema to Define the Domain

The web service domain is defined in an XML schema file (XSD) that Spring-WS will
automatically export as a WSDL.

Create an XSD file with operations to return a country's `name`, `population`, `capital`,
and `currency`. The following listing (from `src/main/resources/META-INF/schemas/countriesWs.xsd`) shows the
necessary XSD file:

[source,xml,indent=0]
----
include::src/main/resources/META-INF/schemas/countriesWs.xsd[]
----

== Generate Domain Classes Based on an XML Schema

The next step is to generate Java classes from the XSD file. The right approach is to do
this automatically during build time by using a Maven  plugin.

The following listing shows the necessary plugin configuration for Maven:

[source,xml,indent=0]
----
include::pom.xml[tags=xsd]
----

Generated classes are placed in the `target/generated-sources/jaxb/` directory.

The JAXB domain object generation process has been wired into the build
tool's lifecycle, so there are no extra steps to run.

== The Country Service Endpoint

To create a service endpoint, you need only a POJO with a few Spring WS annotations to
handle the incoming SOAP requests. The following listing (from
`src/main/java/ch/dboeckli/soap/service/producingwebservice/CountrySoapEndpoint`) shows such a class:

[source,java]
----
include::src/main/java/ch/dboeckli/soap/service/producingwebservice/soap/CountrySoapEndpoint.java[]
----

The `@Endpoint` annotation registers the class with Spring WS as a potential
candidate for processing incoming SOAP messages.

The `@PayloadRoot` annotation is then used by Spring WS to pick the handler
method, based on the message's `namespace` and `localPart`.

The `@RequestPayload` annotation indicates that the incoming message
will be mapped to the method's `request` parameter.

The `@ResponsePayload` annotation makes Spring WS map the returned
value to the response payload.

NOTE: In all of these chunks of code, the `io.spring.guides` classes will report
compile-time errors in your IDE unless you have run the task to generate the domain
classes based on the WSDL.

== Expose the soap service

Now we need to expose the service so that clients can fetch the WSDL definition.
Spring Boot will scan all "*.xsd" and "*.wsdl" files in the configured location.
So first, we'll need to declare the location of our schema files by editing `src/main/resources/application-local.yaml`:

[source,yaml]
----
include::src/main/resources/application-local.yaml[]
----

Spring Boot will create `SimpleXsdSchema` or `DefaultWsdl11Definition` beans with their names matching file names.
Here, we can expect a `SimpleXsdSchema` bean named `countriesWs` and we can use it to create a WSDL bean for exposing it.
For that, let's create a new `src/main/java/ch/dboeckli/soap/service/producingwebservice/config/WebServiceConfig.java` file
with the following content:

[source,java]
----
include::src/main/java/ch/dboeckli/soap/service/producingwebservice/soap/config/WebServiceConfig.java[]
----

The `DefaultWsdl11Definition` exposes a standard WSDL 1.1 by using the configured `XsdSchema`.

IMPORTANT: The bean name for `DefaultWsdl11Definition` determines the URL under
which the generated WSDL file is available. In this case, the WSDL will be available under
`http://<host>:<port>/services/countries.wsdl`.

By default, they will be exposed on the `"/services"` HTTP endpoint, but this can be customized
with the `spring.webservices.path` property.

=== POM sources

* Versions *< 4.15.0*: Spring Boot version from `camel-spring-boot` BOM; CXF version fetched additionally from `camel-parent`.
* Versions *>= 4.15.0*: Both Spring Boot and CXF version from `camel-parent` POM (single fetch).

[source,bash]
----
include::.run/scripts/camel-springboot-matrix.sh[]
----

== OpenTelemetry Resources

Useful links for OpenTelemetry integration with Spring Boot:

- https://uptrace.dev/guides/opentelemetry-spring-boot[OpenTelemetry Spring Boot Guide]
- https://uptrace.dev/get/opentelemetry-java/traceparent[TraceParent in OpenTelemetry Java]

== Test the Application

All HTTP request files are located under `httpRequests/`. Each file contains two variants: one with a `traceparent` header (for distributed tracing) and one without.

=== SOAP V1 — Spring-WS @Endpoint, direct to repository

`httpRequests/soapRequest/getCountryRequest.http`

[source,httprequest]
----
include::httpRequests/soapRequest/getCountryRequest.http[]
----

=== SOAP V2 — Spring-WS @Endpoint, via Camel route

`httpRequests/soapRequest/getCountryRequestV2.http`

[source,httprequest]
----
include::httpRequests/soapRequest/getCountryRequestV2.http[]
----

=== SOAP V3 — Camel spring-ws: entry point, via Camel route

`httpRequests/soapRequest/getCountryRequestV3.http`

[source,httprequest]
----
include::httpRequests/soapRequest/getCountryRequestV3.http[]
----

=== REST — Spring MVC @RestController, direct to repository

`httpRequests/restRequest/getCountryRequest.http`

[source,httprequest]
----
include::httpRequests/restRequest/getCountryRequest.http[]
----

=== Example SOAP Response

All SOAP endpoints return a response in the following format:

[source,xml]
----
<?xml version="1.0"?>
<SOAP-ENV:Envelope xmlns:SOAP-ENV="http://schemas.xmlsoap.org/soap/envelope/">
  <SOAP-ENV:Header/>
  <SOAP-ENV:Body>
    <ns2:getCountryResponse xmlns:ns2="https://spring.io/guides/gs-producing-web-service">
      <ns2:country>
        <ns2:name>Spain</ns2:name>
        <ns2:population>46704314</ns2:population>
        <ns2:capital>Madrid</ns2:capital>
        <ns2:currency>EUR</ns2:currency>
      </ns2:country>
    </ns2:getCountryResponse>
  </SOAP-ENV:Body>
</SOAP-ENV:Envelope>
----

== Sandbox (local dev environment)

The sandbox is provisioned by the opencode-sandbox-kit and runs as a Docker container. It mounts this
repo, starts opencode, and connects the IntelliJ MCP server.

Allow the kit source (GitHub without cloning):

[source,powershell]
----
sbx settings set kit.allowedSources --% "[\"docker.io/\",\"github.com/dboeckli/\"]"
----

Start a new sandbox:

[source,powershell]
----
sbx run opencode --name spring-7-web-services --kit "git+https://github.com/dboeckli/opencode-sandbox-kit.git#dir=opencode-agent" "C:\development\projects\spring-7-web-services"
----

Start the sandbox with Kubernetes support:

[source,powershell]
----
sbx run opencode --name spring-7-web-services --kit "git+https://github.com/dboeckli/opencode-sandbox-kit.git#dir=opencode-agent" "C:\development\projects\spring-7-web-services" "$env:USERPROFILE\.kube:ro"
----

Apply the kit to an existing sandbox (restarts the sandbox, VM state is kept):

[source,powershell]
----
sbx kit add spring-7-web-services "git+https://github.com/dboeckli/opencode-sandbox-kit.git#dir=opencode-agent"
----



